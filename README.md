# CRM Перетяжка Мебели

Веб-приложение для управления заказами по перетяжке мебели с разделением ролей и полным циклом обработки заказов.

## 📋 Описание

Система управления заказами (CRM) для бизнеса по перетяжке мебели. Приложение позволяет:
- Администраторам создавать и управлять заказами
- Логистике добавлять детали заказов (цены, сроки, фотографии)
- Мастерской отмечать выполненные заказы
- Отслеживать полную историю изменений заказов

## 🏗️ Архитектура

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **SQLite** - база данных (с поддержкой асинхронных операций через aiosqlite)
- **JWT** - аутентификация через токены
- **Passlib** - хеширование паролей (pbkdf2_sha256)

### Frontend
- **Next.js 16** - React-фреймворк с App Router
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Radix UI** - компоненты UI
- **Axios** - HTTP-клиент
- **React Hook Form** - управление формами

## 📦 Установка и запуск

### Требования
- Python 3.10+
- Node.js 18+
- npm или yarn

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
```bash
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

5. Инициализируйте базу данных:
```bash
python init_db.py
```

6. Запустите сервер:
```bash
python main.py
```

Сервер будет доступен по адресу `http://localhost:8000`

### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env.local` (опционально):
```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_API_SERVER_URL=http://localhost:8000
```

4. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 👥 Тестовые аккаунты

После инициализации базы данных доступны следующие пользователи:

| Роль | Имя пользователя | Пароль | Описание |
|------|------------------|--------|----------|
| Администратор | admin1 | nimda | Полный доступ |
| Администратор | admin2 | nimda | Полный доступ |
| Логистика | logist | logist | Управление заказами после подтверждения |
| Мастерская | work | work | Выполнение заказов |

## 🔄 Рабочий процесс

1. **Администратор** создает заказ (черновик) с информацией о клиенте
2. Администратор отправляет заказ на подтверждение
3. Администратор подтверждает заказ (присваивается номер заказа)
4. **Логистика** добавляет детали: требования клиента, срок выполнения, цену, фотографии материала и мебели
5. Заказ автоматически переходит в статус "В работе"
6. **Мастерская** видит заказ и после выполнения отмечает его как готовый
7. **Логистика** отмечает заказ как доставленный

## 🗂️ Структура проекта

```
.
├── backend/                 # Backend приложение
│   ├── main.py             # Точка входа FastAPI
│   ├── database.py         # Конфигурация БД
│   ├── models.py           # SQLAlchemy модели
│   ├── init_db.py          # Инициализация БД
│   ├── requirements.txt    # Python зависимости
│   ├── routers/            # API роутеры
│   │   ├── auth.py        # Аутентификация
│   │   └── orders.py      # Управление заказами
│   └── uploads/            # Загруженные файлы
│
├── frontend/               # Frontend приложение
│   ├── src/
│   │   ├── app/           # Next.js страницы
│   │   │   ├── page.tsx   # Главная страница
│   │   │   ├── login/     # Страница входа
│   │   │   └── dashboard/ # Панель управления
│   │   ├── components/    # React компоненты
│   │   ├── contexts/       # React контексты
│   │   │   └── AuthContext.tsx
│   │   └── lib/           # Утилиты
│   │       ├── api.ts     # API клиент
│   │       └── utils.ts
│   ├── package.json
│   └── next.config.ts
│
└── README.md
```

## 🔐 Безопасность

- Пароли хешируются с использованием pbkdf2_sha256
- JWT токены с временем жизни 30 минут
- Проверка ролей на уровне API
- Валидация всех входных данных
- Проверка типов и размеров загружаемых файлов
- CORS настроен для безопасности

## 🧪 Тестирование

### Backend тесты

Для запуска тестов backend:
```bash
cd backend
pytest
```

### Frontend тесты

Для запуска тестов frontend:
```bash
cd frontend
npm test
```

## 🐛 Известные проблемы и исправления

В ходе аудита были исправлены следующие проблемы:

### Backend
- ✅ Заменен устаревший `datetime.utcnow()` на `datetime.now(timezone.utc)`
- ✅ Добавлена асинхронная обработка файлов с использованием `aiofiles`
- ✅ Добавлена валидация типов и размеров файлов
- ✅ Добавлена проверка входных данных во всех эндпоинтах
- ✅ Улучшена обработка ошибок
- ✅ Добавлено явное обновление `updated_at` при изменениях

### Frontend
- ✅ Использование переменных окружения для API URL
- ✅ Улучшена обработка дат (использование datetime-local)
- ✅ Добавлена валидация форм на клиенте
- ✅ Улучшена обработка ошибок с понятными сообщениями
- ✅ Добавлена проверка токена при инициализации приложения
- ✅ Исправлена работа с загруженными изображениями

## 🚀 Деплой

Проект готов к деплою! См. подробные инструкции в:
- `QUICK_START.md` - Быстрый старт
- `DEPLOY.md` - Полная инструкция по деплою
- `DEPLOY_RENDER.md` - **Деплой на Render (рекомендуется)**
- `DEPLOY_CHECKLIST.md` - Чеклист для деплоя

### Быстрый запуск с Docker

```bash
# Локальный запуск
docker-compose up -d --build

# Production запуск
docker-compose -f docker-compose.prod.yml up -d --build
```

### Переменные окружения

**Backend (.env):**
```env
DATABASE_URL=sqlite+aiosqlite:///./furniture_crm.db
SECRET_KEY=your-secret-key-here
PORT=8000
CORS_ORIGINS=http://localhost:3000,http://frontend:3000
```

**Frontend (.env.local):**
```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_API_SERVER_URL=http://localhost:8000
```

**ВАЖНО:** В production используйте сильный `SECRET_KEY` и настройте `CORS_ORIGINS` только для ваших доменов!

## 📝 API Документация

После запуска backend доступна автоматическая документация:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## 🤝 Вклад в проект

При внесении изменений:
1. Следуйте существующему стилю кода
2. Добавляйте комментарии к сложным участкам
3. Обновляйте документацию при необходимости
4. Тестируйте изменения

## 📄 Лицензия

Проект создан для внутреннего использования.

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории проекта.

