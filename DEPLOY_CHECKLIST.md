# Чеклист для деплоя

## ✅ Подготовка к деплою

### Файлы конфигурации
- [x] Dockerfile для backend обновлен
- [x] Dockerfile для frontend обновлен
- [x] docker-compose.yml настроен
- [x] docker-compose.prod.yml создан
- [x] .dockerignore файлы созданы
- [x] .env.example файлы созданы
- [x] .gitignore обновлен

### Переменные окружения
- [ ] Создан `.env` файл для production
- [ ] `SECRET_KEY` изменен на сильный случайный ключ
- [ ] `CORS_ORIGINS` настроен для production доменов
- [ ] `NEXT_PUBLIC_API_URL` настроен для production
- [ ] `DATABASE_URL` проверен

### Безопасность
- [ ] Сильный SECRET_KEY сгенерирован
- [ ] CORS настроен только для нужных доменов
- [ ] HTTPS настроен (SSL/TLS сертификаты)
- [ ] Firewall настроен (открыты только нужные порты)
- [ ] Резервное копирование БД настроено

## 🚀 Деплой

### Локальное тестирование
- [ ] `docker-compose up -d --build` выполнен успешно
- [ ] Frontend доступен на http://localhost:3000
- [ ] Backend доступен на http://localhost:8000
- [ ] API docs работают на http://localhost:8000/docs
- [ ] Тесты проходят: `make test` или `pytest`

### Production деплой
- [ ] Сервер подготовлен (Docker установлен)
- [ ] Код развернут на сервере
- [ ] Переменные окружения настроены
- [ ] `docker-compose -f docker-compose.prod.yml up -d --build` выполнен
- [ ] Health checks проходят успешно
- [ ] Логи проверены на ошибки

## 📋 Проверка после деплоя

### Функциональность
- [ ] Авторизация работает
- [ ] Создание заказов работает
- [ ] Загрузка файлов работает
- [ ] Все роли (admin, logist, work) работают правильно
- [ ] История изменений сохраняется

### Производительность
- [ ] Время загрузки страниц приемлемое
- [ ] API отвечает быстро
- [ ] Нет утечек памяти

### Мониторинг
- [ ] Логи собираются
- [ ] Мониторинг настроен (опционально)
- [ ] Алерты настроены (опционально)

## 🔄 Поддерживание

### Регулярные задачи
- [ ] Резервное копирование БД настроено (автоматическое)
- [ ] Обновления зависимостей проверяются
- [ ] Логи проверяются на ошибки
- [ ] Производительность мониторится

### Полезные команды
```bash
# Просмотр логов
docker-compose logs -f

# Создать резервную копию БД
make backup-db

# Перезапустить приложение
docker-compose restart

# Обновить код
git pull
docker-compose up -d --build
```

