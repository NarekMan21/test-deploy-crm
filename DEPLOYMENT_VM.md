# Развертывание CRMpy на облачной VM (Cloud.ru)

## 📋 Предварительные требования

- Ubuntu/Debian VM с публичным IP
- SSH доступ к VM
- Права администратора

## 🚀 Шаги развертывания

### 1. Подготовка файлов на локальной машине

```bash
# Сохраняем Docker образы
docker save crmpy1-backend:latest -o crm-backend.tar
docker save crmpy1-frontend:latest -o crm-frontend.tar
```

### 2. Загрузка файлов на VM

```bash
# Копируем файлы на VM (замените IP на ваш)
scp crm-backend.tar crm-frontend.tar docker-compose-vm.yml deploy-vm.sh ubuntu@176.108.253.113:~

# Подключаемся к VM
ssh ubuntu@176.108.253.113
```

### 3. Запуск развертывания

```bash
# Делаем скрипт исполняемым и запускаем
chmod +x deploy-vm.sh
./deploy-vm.sh
```

## 🌐 Доступ к приложению

После успешного развертывания приложение будет доступно по адресам:

- **Frontend (веб-интерфейс):** http://176.108.253.113:3000
- **API (бэкенд):** http://176.108.253.113:8080

## 🔑 Тестовые аккаунты

| Роль | Логин | Пароль |
|------|-------|--------|
| Администратор | admin1 | nimda |
| Администратор | admin2 | nimda |
| Логистика | logist | logist |
| Рабочий | work | work |

## 🛠 Управление приложением

```bash
# Просмотр статуса
docker-compose -f docker-compose-vm.yml ps

# Просмотр логов
docker-compose -f docker-compose-vm.yml logs -f

# Перезапуск
docker-compose -f docker-compose-vm.yml restart

# Остановка
docker-compose -f docker-compose-vm.yml down
```

## 💾 Резервное копирование данных

```bash
# База данных
cp backend/furniture_crm.db backup_$(date +%Y%m%d).db

# Загруженные файлы
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz backend/uploads/
```

## 📊 Мониторинг

```bash
# Проверка здоровья API
curl http://localhost:8080/health

# Проверка доступности фронтенда
curl -I http://localhost:3000
```

## 🔧 Устранение неисправностей

### Приложение не запускается
```bash
# Проверить логи
docker-compose -f docker-compose-vm.yml logs

# Проверить ресурсы
df -h
free -h
```

### Проблемы с подключением
```bash
# Проверить открытые порты
netstat -tlnp | grep :3000
netstat -tlnp | grep :8080

# Проверить Docker
docker ps
```

### Проблемы с базой данных
```bash
# Проверить файл базы данных
ls -la backend/furniture_crm.db

# Пересоздать базу данных
rm backend/furniture_crm.db
docker-compose -f docker-compose-vm.yml restart backend
```

## 💰 Стоимость

- **VM (free-tier-2-4):** Бесплатно в рамках Free Tier
- **Диск (30GB SSD):** Включено в Free Tier
- **Трафик:** Бесплатно первые 100GB/месяц

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь в наличии свободных ресурсов
3. Проверьте сетевые настройки
4. Перезапустите сервисы