# 🚀 Финальные инструкции по развертыванию CRMpy на VM Cloud.ru

## ✅ Что уже сделано
- Docker и Docker Compose установлены на VM
- Конфигурационные файлы созданы
- Проект оптимизирован и протестирован

## 📦 Что нужно загрузить на VM

Загрузите следующие файлы в домашнюю директорию `/home/user1/` на VM `176.108.253.113`:

1. **crm-backend.tar** (149MB) - Docker образ бэкенда
2. **crm-frontend.tar** (64MB) - Docker образ фронтенда

### Способы загрузки:

#### Вариант 1: Через веб-консоль Cloud.ru
1. Откройте консоль VM в браузере
2. Используйте кнопку "Загрузить файл"
3. Выберите файлы crm-backend.tar и crm-frontend.tar

#### Вариант 2: Через SCP (если настроен SSH)
```bash
scp crm-backend.tar crm-frontend.tar user1@176.108.253.113:~/
```

#### Вариант 3: Через внешнее хранилище
1. Загрузите файлы на Google Drive/Dropbox/Mega
2. В консоли VM выполните:
```bash
wget "https://ссылка_на_backend.tar"
wget "https://ссылка_на_frontend.tar"
```

## 🚀 Запуск приложения

После загрузки файлов выполните в консоли VM:

```bash
./deploy-vm.sh
```

## 📊 Проверка работы

```bash
# Статус контейнеров
docker-compose -f docker-compose-vm.yml ps

# Логи приложений
docker-compose -f docker-compose-vm.yml logs

# Тест API
curl http://localhost:8080/health
```

## 🌐 Доступ к приложению

**URL:** http://176.108.253.113:3000

**Тестовые аккаунты:**
- **Администратор:** admin1 / nimda
- **Логистика:** logist / logist
- **Рабочий:** work / work

## 🔧 Управление

```bash
# Остановить
docker-compose -f docker-compose-vm.yml down

# Запустить
docker-compose -f docker-compose-vm.yml up -d

# Перезапустить
docker-compose -f docker-compose-vm.yml restart

# Просмотр логов
docker-compose -f docker-compose-vm.yml logs -f
```

## 💾 Резервное копирование

```bash
# База данных
cp backend/furniture_crm.db backup_$(date +%Y%m%d).db

# Файлы
tar -czf uploads_backup.tar.gz backend/uploads/
```

## 🎯 Результат

После успешного развертывания CRMpy будет полностью работоспособен на вашей VM в Cloud.ru с:
- ✅ Полнофункциональным управлением заказами
- ✅ Ролевой системой доступа
- ✅ Загрузкой и хранением изображений
- ✅ Современным веб-интерфейсом
- ✅ Бесплатным хостингом в Free Tier

**Приложение готово к использованию!** 🚀