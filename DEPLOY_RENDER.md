# Деплой на Render

Подробная инструкция по деплою CRM приложения на платформу Render.

## 📋 Предварительные требования

1. Аккаунт на [Render.com](https://render.com/) (можно зарегистрироваться через GitHub)
2. Репозиторий проекта на GitHub (уже готов: https://github.com/NarekMan21/test-deploy-crm)

## 🚀 Вариант 1: Автоматический деплой через Blueprint (рекомендуется)

### Шаг 1: Подготовка

1. Убедитесь, что файл `render.yaml` находится в корне репозитория
2. Все изменения закоммичены и запушены в GitHub

### Шаг 2: Создание Blueprint в Render

1. Войдите в [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"Blueprint"**
3. Подключите ваш GitHub репозиторий `NarekMan21/test-deploy-crm`
4. Render автоматически обнаружит `render.yaml`
5. Проверьте настройки сервисов
6. Нажмите **"Apply"**

### Шаг 3: Настройка переменных окружения

После создания сервисов:

**Backend (`crmpy-backend`):**
1. Перейдите в настройки сервиса
2. В разделе **Environment** обновите:
   - `CORS_ORIGINS`: добавьте URL frontend сервиса
     ```
     https://crmpy-frontend.onrender.com,https://crmpy-backend.onrender.com
     ```
   - `SECRET_KEY`: если не сгенерирован автоматически, создайте сильный ключ:
     ```bash
     python -c "import secrets; print(secrets.token_urlsafe(32))"
     ```

**Frontend (`crmpy-frontend`):**
1. Перейдите в настройки сервиса
2. В разделе **Environment** проверьте:
   - `NEXT_PUBLIC_API_URL` должен указывать на backend
   - `NEXT_PUBLIC_API_SERVER_URL` должен указывать на backend

### Шаг 4: Ожидание деплоя

- Backend обычно деплоится за 3-5 минут
- Frontend деплоится за 5-10 минут

## 🔧 Вариант 2: Ручное создание сервисов

### Backend Service

1. **New +** → **Web Service**
2. Подключите репозиторий `NarekMan21/test-deploy-crm`
3. Настройки:
   ```
   Name: crmpy-backend
   Region: (выберите ближайший к вам)
   Branch: main
   Root Directory: backend
   Runtime: Python 3
   Build Command: pip install -r requirements.txt && python init_db.py
   Start Command: python main.py
   ```
4. **Environment Variables:**
   ```
   DATABASE_URL = sqlite+aiosqlite:///./data/furniture_crm.db
   SECRET_KEY = <сгенерируйте или используйте Generate>
   PORT = 8000
   PYTHON_VERSION = 3.11
   CORS_ORIGINS = (заполните после получения URL)
   ```
5. **Health Check:**
   - Path: `/health`
6. **Add Disk:**
   - Name: `backend-disk`
   - Mount Path: `/app/data`
   - Size: 1GB (достаточно для SQLite)
7. **Plan:** Starter (бесплатно) или Standard/Pro (платно)
8. Нажмите **"Create Web Service"**

### Frontend Service

1. **New +** → **Web Service**
2. Подключите тот же репозиторий
3. Настройки:
   ```
   Name: crmpy-frontend
   Region: (тот же, что и backend)
   Branch: main
   Root Directory: frontend
   Runtime: Node
   Build Command: npm install && npm run build
   Start Command: npm start
   ```
4. **Environment Variables:**
   ```
   NODE_ENV = production
   NEXT_PUBLIC_API_URL = https://crmpy-backend.onrender.com/api
   NEXT_PUBLIC_API_SERVER_URL = https://crmpy-backend.onrender.com
   NEXT_TELEMETRY_DISABLED = 1
   ```
   **Важно:** Замените `crmpy-backend.onrender.com` на реальный URL вашего backend сервиса!
5. **Plan:** Starter (бесплатно) или Standard/Pro (платно)
6. Нажмите **"Create Web Service"**

### Обновление CORS после деплоя Frontend

1. Скопируйте URL вашего frontend сервиса (например: `https://crmpy-frontend.onrender.com`)
2. Перейдите в настройки backend сервиса
3. В **Environment Variables** обновите `CORS_ORIGINS`:
   ```
   https://crmpy-frontend.onrender.com,https://crmpy-backend.onrender.com
   ```
4. Нажмите **"Save Changes"** - backend автоматически перезапустится

## ✅ Проверка после деплоя

### Backend
1. Откройте URL backend: `https://crmpy-backend.onrender.com`
2. Должна быть страница: `{"message": "CRM Furniture API", "status": "healthy"}`
3. Проверьте health check: `https://crmpy-backend.onrender.com/health`
4. Проверьте API docs: `https://crmpy-backend.onrender.com/docs`

### Frontend
1. Откройте URL frontend: `https://crmpy-frontend.onrender.com`
2. Должна загрузиться страница логина
3. Попробуйте войти с тестовыми аккаунтами:
   - `admin1` / `nimda`
   - `logist` / `logist`
   - `work` / `work`

## 🔄 Обновление приложения

Render автоматически деплоит изменения при push в `main` ветку GitHub репозитория.

Для ручного деплоя:
1. Перейдите в настройки сервиса
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**

## 💰 Тарифы Render

### Starter (Free)
- ✅ 750 часов в месяц (достаточно для одного сервиса)
- ✅ 512MB RAM
- ✅ Автоматический HTTPS
- ⚠️ Сервис "засыпает" после 15 минут неактивности (первый запрос может занять 30-50 сек)

### Standard ($7/месяц за сервис)
- ✅ Сервис всегда онлайн
- ✅ Больше RAM
- ✅ Больше CPU

### Pro ($25+/месяц за сервис)
- ✅ Выделенные ресурсы
- ✅ Приоритетная поддержка

**Рекомендация:** Начните с Free тарифа для тестирования. Для production используйте хотя бы Standard план для backend.

## 🐛 Устранение проблем

### Проблема: Backend не запускается

1. Проверьте логи: **Logs** в настройках сервиса
2. Убедитесь, что все зависимости установлены
3. Проверьте, что `SECRET_KEY` установлен
4. Проверьте health check путь: `/health`

### Проблема: Frontend не может подключиться к Backend

1. Проверьте `NEXT_PUBLIC_API_URL` в настройках frontend
2. Проверьте `CORS_ORIGINS` в настройках backend - должен включать URL frontend
3. Проверьте, что backend сервис запущен

### Проблема: База данных не сохраняется

1. Убедитесь, что Disk добавлен и примонтирован
2. Проверьте `DATABASE_URL` - путь должен быть внутри mount path диска
3. Проверьте логи на наличие ошибок доступа к файлу БД

### Проблема: Медленный первый запрос (Free тариф)

Это нормально - сервис "просыпается". Для решения перейдите на Standard план.

## 📊 Мониторинг

Render предоставляет:
- Логи в реальном времени
- Метрики использования ресурсов
- История деплоев
- Webhooks для интеграций

## 🔒 Безопасность

1. **SECRET_KEY:** Используйте сильный случайный ключ (Render может сгенерировать автоматически)
2. **CORS:** Настройте только нужные домены
3. **HTTPS:** Автоматически включен на Render
4. **Environment Variables:** Чувствительные данные храните в Environment, не коммитьте в код

## 📞 Поддержка

- [Render Documentation](https://render.com/docs)
- [Render Community](https://community.render.com/)
- [Render Status](https://status.render.com/)

## 🎯 Чеклист деплоя

- [ ] Репозиторий подключен к Render
- [ ] Backend сервис создан и запущен
- [ ] Frontend сервис создан и запущен
- [ ] Environment variables настроены
- [ ] CORS обновлен с URL frontend
- [ ] Health checks проходят успешно
- [ ] Frontend может подключиться к backend
- [ ] Тестовые аккаунты работают
- [ ] Disk добавлен для backend (для сохранения БД)

Готово! Ваше приложение развернуто на Render 🚀

